
if not Harvest.Data then
	Harvest.Data = {}
end

local Data = Harvest.Data


function Data:LogNode(map, globalX, globalY, z, zoneId, pinTypeId)
	-- feature was removed
	-- it might come back in the future
end
